Manages an n8n Multiple Headers Auth credential (`httpMultipleHeadersAuth`) via the Public API.

`headers` is a write-only object matching n8n's fixed collection: `{ values = [{ name, value }] }`. Used by HTTP Request and as an MCP Authentication option. Terraform 1.11 or later is required.
