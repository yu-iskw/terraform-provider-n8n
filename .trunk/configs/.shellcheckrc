enable=all
source-path=SCRIPTDIR
disable=SC2154
# Intentional: capture HTTP helper stdout then inspect HTTP_CODE separately.
disable=SC2312
# MODE is validated earlier; default case is unreachable.
disable=SC2249

# If you're having issues with shellcheck following source, disable the errors via:
# disable=SC1090
# disable=SC1091
